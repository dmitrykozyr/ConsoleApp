# Анализ соответствия архитектуре DDD (Domain-Driven Design)

## 🔴 Критические нарушения принципов DDD

### 1. Domain слой содержит реализации сервисов

**Проблема:** В Domain слое находятся конкретные реализации сервисов, которые должны быть в Application или Infrastructure.

**Нарушения:**
- `Domain/Services/SqlService.cs` - реализация сервиса работы с БД
- `Domain/Services/API/DbService.cs` - реализация бизнес-логики
- `Domain/Services/API/FilesService.cs` - реализация работы с файлами
- `Domain/Services/Login/LoginService.cs` - реализация логики аутентификации

**Правильно:** Domain должен содержать только:
- Интерфейсы (репозитории, сервисы)
- Сущности (Entities)
- Value Objects
- Domain Events
- Domain Services (только чистую бизнес-логику без зависимостей от инфраструктуры)

**Решение:** Переместить реализации в Application или Infrastructure слой.

---

### 2. Domain зависит от инфраструктурных технологий

**Проблема:** Domain слой имеет зависимости от конкретных технологий, что нарушает изоляцию домена.

**Нарушения:**

#### 2.1. Зависимость от Entity Framework Core
```csharp
// Domain/Interfaces/Db/DbContext/IApplicationDbContext.cs
using Microsoft.EntityFrameworkCore;  // ❌ НЕ ДОЛЖНО БЫТЬ В DOMAIN
using Microsoft.EntityFrameworkCore.Infrastructure;  // ❌ НЕ ДОЛЖНО БЫТЬ В DOMAIN

public interface IApplicationDbContext
{
    DbSet<Customer> Customers { get; set; }  // ❌ DbSet - это EF Core
    DatabaseFacade Database { get; }  // ❌ DatabaseFacade - это EF Core
}
```

**Правильно:** Domain не должен знать о EF Core. Интерфейс должен быть абстрактным:
```csharp
public interface IApplicationDbContext
{
    // Абстрактные методы без упоминания EF Core
}
```

#### 2.2. Зависимость от ASP.NET Core
```csharp
// Domain/Services/API/FilesService.cs
using Microsoft.AspNetCore.Http;  // ❌ НЕ ДОЛЖНО БЫТЬ В DOMAIN

public async Task<Guid> LoadFileFromFileSystemBySelection(
    LoadFileBySelectionRequest model, 
    IFormFile file)  // ❌ IFormFile - это ASP.NET Core
```

**Правильно:** Использовать абстрактные типы или DTO без зависимостей от веб-фреймворка.

#### 2.3. Зависимость от System.Data.SqlClient
```csharp
// Domain/Services/SqlService.cs
using System.Data.SqlClient;  // ❌ Инфраструктурная деталь

public SqlConnection? CreateConnection()  // ❌ SqlConnection - конкретная реализация
```

**Правильно:** Использовать абстрактные интерфейсы для работы с БД.

---

### 3. Application слой пустой

**Проблема:** Слой Application не содержит никаких классов, только пустой `.csproj` файл.

**Правильно:** Application слой должен содержать:
- Use Cases / Application Services
- Command Handlers / Query Handlers (если используется CQRS)
- DTO для передачи данных между слоями
- Application-specific логику (оркестрация доменных сервисов)

**Текущее состояние:**
- Вся бизнес-логика находится в Domain слое
- Нет разделения между Domain логикой и Application логикой

**Решение:** 
- Переместить `DbService`, `FilesService` и другие application services в Application слой
- Оставить в Domain только чистую доменную логику

---

### 4. Domain содержит инфраструктурные детали

**Проблема:** Domain слой содержит код, который относится к инфраструктуре.

**Примеры:**
- `Domain/Services/SqlService.cs` - работа с SQL соединениями
- `Domain/Services/API/FilesService.cs` - HTTP запросы, работа с файловой системой
- Использование `HttpWebRequest`, `FileStream`, `Path.Combine` и т.д.

**Правильно:** 
- Domain должен содержать только бизнес-логику
- Вся работа с внешними системами (HTTP, файлы, БД) должна быть в Infrastructure

---

### 5. Сущности Domain не соответствуют принципам DDD

**Проблема:** Сущности `Customer` и `Order` являются простыми POCO классами без бизнес-логики.

```csharp
// Domain/Models/Db/Customer.cs
public class Customer
{
    public long Id { get; set; }  // ❌ public setter
    public string? Name { get; set; }  // ❌ public setter
    public string? Address { get; set; }  // ❌ public setter
}
```

**Проблемы:**
- Публичные setters позволяют изменять состояние извне
- Нет инкапсуляции бизнес-логики
- Нет валидации на уровне домена
- Нет методов для работы с сущностью

**Правильно:**
```csharp
public class Customer
{
    public long Id { get; private set; }  // ✅ private setter
    public string Name { get; private set; }  // ✅ private setter
    
    // Бизнес-логика
    public void ChangeAddress(string newAddress)
    {
        if (string.IsNullOrWhiteSpace(newAddress))
            throw new DomainException("Address cannot be empty");
        Address = newAddress;
    }
}
```

---

## ⚠️ Проблемы, требующие внимания

### 1. Неправильная структура зависимостей

**Текущая структура:**
```
Presentation → Application, Infrastructure
Application → Domain
Infrastructure → Domain
```

**Проблема:** Presentation зависит напрямую от Infrastructure, что может привести к обходу Application слоя.

**Правильно:** Presentation должен зависеть только от Application:
```
Presentation → Application
Application → Domain
Infrastructure → Domain, Application (для реализации интерфейсов)
```

---

### 2. Интерфейсы в Domain содержат инфраструктурные типы

**Пример:**
```csharp
// Domain/Interfaces/Db/DbContext/IApplicationDbContext.cs
public interface IApplicationDbContext
{
    DbSet<Customer> Customers { get; set; }  // ❌ DbSet - это EF Core
}
```

**Правильно:** Интерфейсы должны быть абстрактными:
```csharp
public interface ICustomerRepository
{
    Task<Customer?> GetByIdAsync(long id, CancellationToken ct);
    Task AddAsync(Customer customer, CancellationToken ct);
    Task SaveChangesAsync(CancellationToken ct);
}
```

---

### 3. Отсутствие Value Objects

**Проблема:** Нет примеров Value Objects, которые являются важной частью DDD.

**Правильно:** Использовать Value Objects для инкапсуляции бизнес-правил:
```csharp
public record Email
{
    public string Value { get; }
    
    public Email(string value)
    {
        if (!IsValid(value))
            throw new DomainException("Invalid email");
        Value = value;
    }
    
    private static bool IsValid(string email) { /* ... */ }
}
```

---

### 4. Отсутствие Domain Events

**Проблема:** Нет механизма Domain Events для обработки побочных эффектов.

**Правильно:** Реализовать паттерн Domain Events:
```csharp
public interface IDomainEvent { }
public class CustomerCreatedEvent : IDomainEvent { }
```

---

## ✅ Что сделано правильно

### 1. Правильная структура слоёв
- Есть разделение на Domain, Application, Infrastructure, Presentation
- Infrastructure правильно зависит только от Domain
- Application правильно зависит только от Domain

### 2. Интерфейсы репозиториев в Domain
- `ICustomerRepository` определен в Domain слое
- Это правильный подход - Domain определяет контракты

### 3. Использование интерфейсов
- Есть интерфейсы для сервисов (`IFilesService`, `ISqlService`, `ILogging`)
- Это позволяет инверсию зависимостей

---

## 📋 Рекомендации по исправлению

### Приоритет 1 (Критично)

1. **Переместить реализации сервисов из Domain в Application/Infrastructure**
   - `SqlService` → Infrastructure
   - `FilesService` → Application (если содержит бизнес-логику) или Infrastructure (если только техническая работа)
   - `DbService` → Application
   - `LoginService` → Application

2. **Убрать зависимости от EF Core из Domain**
   - Переписать `IApplicationDbContext` без использования `DbSet`
   - Использовать абстрактные интерфейсы репозиториев

3. **Убрать зависимости от ASP.NET Core из Domain**
   - Заменить `IFormFile` на абстрактный тип или DTO
   - Убрать `using Microsoft.AspNetCore.Http`

4. **Создать Application слой**
   - Переместить application services в Application
   - Реализовать use cases / handlers

### Приоритет 2 (Важно)

5. **Улучшить сущности Domain**
   - Сделать setters private
   - Добавить методы для изменения состояния
   - Добавить валидацию на уровне домена

6. **Исправить зависимости Presentation**
   - Presentation не должен зависеть напрямую от Infrastructure
   - Все зависимости должны идти через Application

### Приоритет 3 (Улучшения)

7. **Добавить Value Objects**
   - Для Email, Address, Money и т.д.

8. **Реализовать Domain Events**
   - Для обработки побочных эффектов

9. **Добавить Aggregates**
   - Если есть сложные связи между сущностями

---

## 📊 Сводная таблица нарушений

| Категория | Количество | Приоритет |
|-----------|------------|-----------|
| Реализации в Domain | 4+ класса | Критично |
| Зависимости от инфраструктуры | 3+ типа | Критично |
| Пустой Application слой | 1 | Критично |
| Неправильные сущности | 2+ класса | Важно |
| Неправильные зависимости | 1 | Важно |

---

## 🎯 Итоговая оценка

**Текущее состояние:** ⚠️ **Частичное соответствие DDD**

**Оценка по компонентам:**
- Структура слоёв: ✅ Хорошо
- Изоляция Domain: ❌ Плохо (критические нарушения)
- Application слой: ❌ Отсутствует
- Infrastructure: ✅ Хорошо
- Сущности: ⚠️ Требуют улучшения

**Общая оценка:** 3/10

Проект имеет правильную структуру слоёв, но Domain слой содержит слишком много инфраструктурных деталей и реализаций, что нарушает основные принципы DDD. Требуется рефакторинг для приведения к стандартам DDD.



