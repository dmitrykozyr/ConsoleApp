{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  // Если в SSMS для подключения к серверу используем WindowsAunthentication, то тут в ConnectionString
  // логин и пароль писать не нужно, а если указан 'SQL Server Authentication', то нужно
  "ConnectionStrings": {

    // Чтобы посмотреть ConnectionString, открываем окно 'SQL Server Object Explorer', выбираем нужный сервер
    // и в окне 'Properties' смотрим параметры ConnectionString

    // В программе 'SQL Server Configuration Manager' -> Protocols for SQLEXPRESS -> TCP/IT -> IP Adresses
    // -> IPALL -> TCP Port указать 1433, в меню SQL Server Services перезагрузить сервер после изменений
    "DefaultConnection": "Server=LAPTOP-IS5VCQCB\\SQLEXPRESS;Database=KozyrDb;Trusted_Connection=True;"
  }
}
