@model Category

<form method="post">
    <div calss="border p-3 mt-4">

        <div class="row pb-2">
            <h2 class="text-primary">Create Category</h2>
            <hr />
        </div>

        @* Отображаем списком все сообщения об ошибках валидации *@
        @*<div asp-validation-summary="All"></div>*@

        <div class="mb-3">
            <label asp-for="Name"></label>
            <input asp-for="Name" class="form-control" />

            @* Отображаем сообщения валидации при неправильном заполнении поля Name *@
            <span asp-validation-for="Name" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label asp-for="DisplayOrder"></label>
            <input asp-for="DisplayOrder" class="form-control" />

            @* Отображаем сообщения валидации при неправильном заполнении поля DisplayOrder *@
            <span asp-validation-for="DisplayOrder" class="text-danger"></span>
        </div>

        <button type="submit" class="btn btn-primary" style="width:150px">Create</button>
        <a asp-controller="Category" asp-action="Index" class="btn btn-secondary" style="width:150px">
            Back to list
        </a>

    </div>
</form>

@*
    Подключеам PartialViev, где подключаются скрипты, отвечающие за валидацию на стороне клиента
    Теперь страница не будет перезагружаться, если сработает валидация
    Но это не распространяется на прописанную нами кастомную валидацию, как в экшене Create в CategoryController
*@

@section Scripts{
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}
